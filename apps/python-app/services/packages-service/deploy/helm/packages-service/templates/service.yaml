apiVersion: v1
kind: Service
metadata:
  name: {{ include "packages-service.fullname" . }}
  labels:
    {{- include "packages-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "packages-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
