apiVersion: v1
kind: Service
metadata:
  name: {{ include "issues-service.fullname" . }}
  labels:
    {{- include "issues-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "issues-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
