apiVersion: v1
kind: Service
metadata:
  name: {{ include "dashboard-web.fullname" . }}
  labels:
    {{- include "dashboard-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "dashboard-web.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
